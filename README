This is the web application which runs www.tntnet.org (not yet - will be soon)

The project is built using autotools. Run ./autogen.sh; ./configure; make to bild the project.

To run the application, change to the webapp directory and enter "tntnet".
You find your webapplication on port 8000.
